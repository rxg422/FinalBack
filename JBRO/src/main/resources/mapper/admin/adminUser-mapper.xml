<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jbro.admin.adminUserMapper">

    <select id="selectUsers" parameterType="map" resultType="com.jbro.admin.model.dto.AdminUserDto$UserItem">
        SELECT
            NO as no,
            EMAIL as email,
            NICKNAME as nickname,
            ROLE as role,
            STATUS as status,
            DATE_FORMAT(CREATED_AT, '%Y-%m-%d') as createdAt
        FROM USERS
        <where>
            <if test="keyword != null">
                AND (NICKNAME LIKE CONCAT('%', #{keyword}, '%')
                OR EMAIL LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND STATUS = #{status}
            </if>
        </where>
        ORDER BY CREATED_AT DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectUserCount" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        <where>
            <if test="keyword != null">
                AND (NICKNAME LIKE CONCAT('%', #{keyword}, '%')
                OR EMAIL LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND STATUS = #{status}
            </if>
        </where>
    </select>
    
    <update id="updateUserStatus" parameterType="map">
    UPDATE USERS
    SET STATUS = #{status}
    WHERE NO = #{no}
    </update>

</mapper>